%h1 Login

= simple_form_for :session, method: :post, url: sessions_path, html: { :class => 'form-horizontal' }  do |f|
	%fieldset
		
		= f.input :username, :label => 'Username'
		= f.input :password, :label => 'Password'
		
		.actions
			= f.button :submit, :value => 'Login'